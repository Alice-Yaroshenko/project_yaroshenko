def Minmax(x, y):
    if x <= y:
        return x, y
    else:
        return y, x


# Основная программа
try:
    A = float(input("Введите A: "))
…    minimum = A
    maximum = D

    print(minimum)
    print(maximum)

except:
    print("ошибка: введено не число")
